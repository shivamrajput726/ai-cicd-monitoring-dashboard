apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-cicd-monitor-ingress
  namespace: ai-cicd-monitor
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: ai-cicd.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80

